{
  "name": "wordcount",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-core": "cd core && wasm-pack build && rimraf ./pkg/.gitignore && cd .."
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.4",
    "vite": "^3.2.0",
    "vite-plugin-top-level-await": "^1.2.1",
    "vite-plugin-wasm": "^3.1.0"
  },
  "dependencies": {
    "@fontsource/inter": "^4.5.14"
  }
}
